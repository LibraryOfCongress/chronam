from distutils.core import setup
import os


setup(name='chronam-core',
      version="0.0.0",
      description='',
      author='',
      author_email='',
      url='',
      download_url='',
      packages=['chronam', 'chronam.core', 'chronam.utils'],
      requires=['feedparser', 'rfc3339']
      )
